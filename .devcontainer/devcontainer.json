{
  "name": "PRINTME! Dev Environment",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "postCreateCommand": "pip install -r requirements.txt && git config core.hooksPath .githooks && chmod +x .githooks/pre-commit",
  "forwardPorts": [5000],
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}